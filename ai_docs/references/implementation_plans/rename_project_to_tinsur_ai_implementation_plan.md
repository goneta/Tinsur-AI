# Rename Project to Tinsur.AI

This plan outlines the steps to rename the project from "Insurance SaaS" to "Tinsur.AI" across the codebase.

## User Review Required

> [!IMPORTANT]
> **Manual Action Required**: You will need to manually rename the GitHub repository to `Tinsur.AI` (or `Tinsur-AI`) and the local folder `Insurance SaaS` to `Tinsur.AI`. I cannot perform these actions safely/automatically from here.

## Proposed Changes

### Frontend
#### [MODIFY] [layout.tsx](file:///c:/Users/user/Desktop/Insurance SaaS/frontend/app/layout.tsx)
- Update metadata title from "Insurance SaaS" to "Tinsur.AI".

#### [MODIFY] [package.json](file:///c:/Users/user/Desktop/Insurance SaaS/frontend/package.json)
- Update package name to `tinsur-ai-frontend` (or similar).

### Backend
#### [MODIFY] [config.py](file:///c:/Users/user/Desktop/Insurance SaaS/backend/app/core/config.py)
- Update `PROJECT_NAME` or equivalent configuration to "Tinsur.AI".

### Documentation
#### [MODIFY] [README.md](file:///c:/Users/user/Desktop/Insurance SaaS/backend/README.md)
- Update project title references.

## Verification Plan

### Manual Verification
1.  **Verify Frontend**: Check `package.json` and `layout.tsx` content.
2.  **Verify Backend**: Check `config.py` content.
3.  **Remote URL**: Update git remote URL to new GitHub path (after user renames it).
