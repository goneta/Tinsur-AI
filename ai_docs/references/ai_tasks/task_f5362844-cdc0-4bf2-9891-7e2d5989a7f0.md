# Task: Fix Database Migrations (Phase 7 & Claims)

- [/] Analyze Database and Migration State
    - [x] Review existing Alembic migrations
    - [ ] Check current database schema (using inspection script)
    - [ ] Identify discrepancies between models and migrations
- [ ] Implement Migration Fix
    - [ ] Create a new Alembic migration to:
        - [ ] Re-create `claims` table correctly
        - [ ] Create missing Phase 7 tables: `inter_company_shares`, `tickets`, `referrals`, `loyalty_points`, `telematics_data`, `ml_models`
    - [ ] Test migration locally (upgrade/downgrade)
- [ ] Clean up redundant `create_all` logic
    - [ ] Modify `main.py` to remove `Base.metadata.create_all` if appropriate
- [ ] Verify System after Migration
    - [ ] Run `verify_lifecycle.py` and `verify_advanced_logic.py` to ensure no regressions
